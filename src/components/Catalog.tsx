import React from 'react';

type Props = {};

const Catalog = (props: Props) => {
  return (
    <main className="main">
      <div className="main-wrapper container"></div>
    </main>
  );
};

export default Catalog;
